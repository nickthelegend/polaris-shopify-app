export default function index() {
    return (
        <div>
            <h1>Polaris Admin Panel</h1>
            <p>Configure your Polaris API Keys here.</p>
            {/* You would build a real Remix Form here to store API keys in Metafields or Shopify App state */}
        </div>
    );
}
